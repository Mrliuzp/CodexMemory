# Codex 记忆自动记录

在这个仓库中，把记忆记录当作默认行为。

在每次助手回复之前：

1. 解析仓库的 `project_id`。
2. 为当前线程复用一个稳定的 `conversation_id`。
3. 当任务需要项目历史或前文时，调用 `context`。
4. 在生成回复之前，先对用户轮次调用 `append`。
5. 生成回复。
6. 在生成回复之后，再对助手轮次调用 `append`。

除非有明确理由延迟处理，否则使用 `process_now: true`。
所有写入都必须限制在项目范围内，不要跨越项目边界。
